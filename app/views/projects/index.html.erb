<div class="container">
  <div class="col-md-8">

    <h3>Your Projects</h3><br>

    <% @projects.each do |project| %>
    <h4><%= link_to project.name, project_path(project) %></h4>
    <% end %>

  </div>
</div>
