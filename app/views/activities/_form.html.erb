<%= form_for ([@project, @activity]) do |f| %>

<div class="form-group">
  <%= f.text_area :name, placeholder: "Name", class: "form-control" %>
</div>

<div class="form-group">
  <%= f.submit class: "btn btn-primary" %>
</div>
<% end %>
